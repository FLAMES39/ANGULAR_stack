import { SecondDirectiveDirective } from './second-directive.directive';

describe('SecondDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
